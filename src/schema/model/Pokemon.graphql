type Pokemon {
  id: Int!
  name: String!
  primaryType: String
  secondaryType: String
  stats: Stats
  moves: [Moves]
}

type Stats {
  hp: Int
  attack: Int
  defense: Int
  speed: Int
}

type Moves {
  name: String
  damage: Int
  accuracy: Int
  type: String
  pp: Int
  priority: Boolean
}
